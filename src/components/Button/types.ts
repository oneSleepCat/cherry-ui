export enum ButtonType {
  Primary = "primary",
  Default = "default",
  Danger = "danger",
  Link = "link",
}

export enum ButtonSize {
  Large = "lg",
  Small = "sm",
}

export enum ButtonShape {
  Round = "round",
  Square = "square",
}
